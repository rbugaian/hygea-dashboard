<% content_for :nav_sidebar do %>
	<div class="col-sm-3 col-md-2 sidebar">
		<ul class="nav nav-sidebar">
			<li><a href="/tasks_section">Tasks</a></li>
			<li class="active"><a href="/personnel_section">Personnel<span class="sr-only">(current)</span></a></li>
			<li><a href="/patients_section">Patients</a></li>
			<li><a href="/wards_section">Wards</a></li>
		</ul>
	</div>
<% end %>

<%= content_for :sections do %>

<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
  <h1 class="page-header">Dashboard</h1>
	
	<h2 class="sub-header">Personnel <%= link_to 'Create', {controller: 'personnel', action: 'create'}, class: 'btn btn-default pull-right create-button'  %> </h2>
	
	<div class="table-responsive">
	  <table class="table table-striped">
	    <thead>
	      <tr>
	        <th>#</th>
	        <th>Name</th>
			<th>Email</th>
	        <th>Created at</th>
	        <th>Updated at</th>
	      </tr>
	    </thead>
	    <tbody>

	    <% Personnel.all.each do |personnel| %>
	      <tr>
	        <td><%= personnel.id %></td>
	        <td><%= link_to (personnel.first_name.to_s + ' ' + personnel.last_name.to_s) , { controller: 'personnel', action: 'show_personnel', id: personnel } %> </td>
			<td><%= personnel.email %></td>
	        <td><%= personnel.created_at %></td>
	        <td><%= personnel.updated_at %></td>
	      </tr>
	    <% end %>
     
	    </tbody>
	  </table>
	</div>
  
  
</div>

<% end %>

<%= render template: 'dashboard/show'  %>